<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sakkawy.medicare">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.DetailActivity"></activity>
        <activity android:name=".View.SubActivity" />
        <activity android:name=".View.MessageActivity" />
        <activity android:name=".View.MessageActivity"></activity>
        <activity android:name=".View.PateintHomeInfo"></activity>
        <activity android:name=".View.AddNewPrecription" />
        <activity android:name=".View.DisplayDoctorPrescritions" />
        <activity android:name=".View.AddMedicineActivity" />
        <activity android:name=".View.AddEventActivity" />
        <activity android:name=".View.SettingsActivity" />
        <activity android:name=".View.DoctorHomeActivity" />
        <activity android:name=".View.SubFolderActivity" />
        <activity
            android:name=".View.HomeActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".View.RegistrationActivity" />
        <activity android:name=".View.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".Notifications.MyFirebaseIdService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name=".Notifications.MyFirebaseMessaging"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>